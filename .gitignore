# Dependencies
/node_modules

# Next.js build output
/.next
/out

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Local env (never commit real secrets)
.env
.env.local

# Runtime upload storage (fictitious teaching files only)
/uploads/*
!/uploads/.gitkeep

# Postgres docker volume artefacts (if any leak locally)
/pgdata

# Logs / OS cruft
*.log
.DS_Store
Thumbs.db
server.log
prod.log

# Agent/editor tooling state — not part of the submission
.claude/

# The submission archive is BUILT from the tracked tree; never track it
2021-1-84333CF_IFT542.zip

# PDF export intermediate — the submitted PDF lives at the repo root
/report/*.pdf
