LOCAL, APP-ONLY stored-XSS payloads for THIS app's profile "Display name" field.

[FIXED — Task 3] These payloads NO LONGER EXECUTE. src/app/dashboard/page.tsx and
src/app/profile/page.tsx now render {user.display_name} as a text node, so the
markup below is displayed literally instead of being parsed.

They are kept because they are still the right way to DEMONSTRATE the fix: paste
one in, save, and observe that the page shows the tag as text rather than firing
an alert. Note the value is still stored VERBATIM in the database — encoding
happens at output, which is the point. Confirm with:

  docker compose exec postgres psql -U ift542 -d ift542 -c "SELECT display_name FROM profiles WHERE email='ada.learner@campus.local';"

Originally: [VULN: Stored XSS — Task 3]  (src/app/api/profile/route.ts stored it
raw; both pages rendered it with dangerouslySetInnerHTML).

How to reproduce (localhost only):
  1. npm run dev, then log in at http://127.0.0.1:3000/login
     (e.g. ada.learner@campus.local / ada-pw-2025).
  2. Go to /profile and paste one payload below into "Display name", save.
  3. Load /dashboard (or /profile). BEFORE the fix the payload executed, because
     the value was rendered as raw HTML. AFTER the fix it is shown as text.

Payloads:
  <img src=x onerror="alert('xss-on-dashboard')">
  <img src=x onerror="document.title='XSS'">
  <svg onload="alert(document.cookie)">
  <b style="color:red">not-really-my-name</b>

Reset afterwards: set the display name back to plain text (e.g. "Ada Learner")
or re-run `npm run db:reset`.
